# Font and Asset Licenses

Run `npm run prepare` to copy every pinned project-local font shard and write
its SHA-256 and license binding. Record every non-font Release asset in
`asset-manifest.json`; an empty `assets` array is valid when the poster uses no
external assets.
